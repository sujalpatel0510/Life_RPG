# Backend API URL (optional in local dev, defaults to '/api' via Vite proxy)
# In production (e.g. deployed on Vercel), set this to your deployed backend URL:
# VITE_API_URL="https://life-rpg-server.onrender.com"
VITE_API_URL=""
